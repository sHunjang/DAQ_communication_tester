"""
=================================================================
파일명: virtual-data-pc/config/config.py
설명: 가상 센서 PC 설정 파일
작성자: 개발팀
작성일: 2025-10-21
=================================================================
"""

# =================================================================
# 실행 모드 설정
# =================================================================
# True: Mock Serial 사용 (가상 테스트)
# False: 실제 RS485 하드웨어 사용
USE_MOCK_SERIAL = True

# =================================================================
# RS485 시리얼 통신 설정
# =================================================================
# Mock 모드일 때 사용할 포트
MOCK_PORT_SENSOR = 'COM4'

# 실제 RS485 모드일 때 사용할 포트
REAL_PORT_SENSOR = 'COM4'

# 현재 모드에 따라 포트 선택
if USE_MOCK_SERIAL:
    RS485_PORT = MOCK_PORT_SENSOR
else:
    RS485_PORT = REAL_PORT_SENSOR

# 통신 속도 (bps) - 송신측과 동일해야 함
BAUDRATE = 9600

# 데이터 비트 (일반적으로 8비트 사용)
BYTESIZE = 8

# 패리티 체크
# 'N': None (없음), 'E': Even (짝수), 'O': Odd (홀수)
PARITY = 'N'

# 정지 비트 (1 또는 2)
STOPBITS = 1

# 읽기 타임아웃 (초)
# 서버는 짧은 타임아웃을 사용 (빠른 응답성)
TIMEOUT = 1

# =================================================================
# 가상 센서 설정
# =================================================================
# 지원하는 센서 타입 목록
SENSOR_TYPES = ['temperature', 'humidity', 'pressure', 'voltage']

# 센서 데이터 생성 지연 시뮬레이션 (밀리초)
# 실제 센서의 응답 지연을 시뮬레이션
DATA_GENERATION_DELAY_MS = 10

# 센서 데이터 범위 설정
SENSOR_RANGES = {
    'temperature': {'min': 23.0, 'max': 27.0, 'unit': '℃'},
    'humidity': {'min': 55.0, 'max': 65.0, 'unit': '%'},
    'pressure': {'min': 1003.0, 'max': 1023.0, 'unit': 'hPa'},
    'voltage': {'min': 3.2, 'max': 3.4, 'unit': 'V'}
}

# =================================================================
# 로그 경로
# =================================================================
# 로그 파일 저장 경로
LOG_DIR = 'logs'

# =================================================================
# 프로토콜 설정 (추후 확장용)
# =================================================================
# Slave ID (Modbus 사용 시)
SLAVE_ID = 1

# 프로토콜 타입
PROTOCOL_TYPE = 'CUSTOM'  # 'CUSTOM', 'MODBUS', 'ASCII'
